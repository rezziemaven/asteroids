[project]
name = "asteroids"
version = "0.1.0"
description = "Pygame version of the hit arcade classic Asteroids"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
